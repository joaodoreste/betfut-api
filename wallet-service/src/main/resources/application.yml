server:
  port: 8083

spring:
  application:
    name: wallet-service

  data:
    mongodb:
      uri: mongodb://localhost:27017/betfut_wallet_db

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/