server:
  port: 8082

spring:
  application:
    name: match-service

  datasource:
    url: jdbc:postgresql://localhost:5432/betfut_matches_db
    username: postgres
    password: 12345678

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/